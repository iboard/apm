// Generated by CoffeeScript 1.10.0
(function() {
  this.randomColor = function(transparent) {
    var col, i, len, ref, rgb;
    if (transparent == null) {
      transparent = false;
    }
    rgb = [];
    ref = ['r', 'g', 'b'];
    for (i = 0, len = ref.length; i < len; i++) {
      col = ref[i];
      rgb.push(Math.round(Math.random() * 255));
    }
    if (transparent) {
      return "rgba(" + rgb[0] + "," + rgb[1] + "," + rgb[2] + "," + (Math.random()) + ")";
    } else {
      return "rgb(" + rgb[0] + "," + rgb[1] + "," + rgb[2] + ")";
    }
  };

}).call(this);
